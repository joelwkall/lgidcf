// extern crate piston_window;
// extern crate piston;

// use piston_window::*;
// use app::*;

// pub trait GameObject{

	// fn render(&self, c:&Context, g: &mut G2d, args: &RenderArgs);

    // fn update(&self, args: &UpdateArgs, data: &AppData) -> Option<Box<GameObject>>;
	
	// fn is_outside(&self, data: &AppData) -> bool {
		// //return self.x<0.0 || self.x > (data.width as f64) || self.y<0.0 || self.y > (data.height as f64);
		
		// return false;
	// }
	
	// fn get_new_speed(&self, args: &UpdateArgs) {
		// let (_,speed_y) = self.get_speed();
		// return speed_y + 9.81*args.dt*PIXELS_PER_METER;
	// }
	
	// fn get_speed(&self) -> (f64,f64);
	
	// fn get_position(& self) -> (f64,f64);
	
// }



